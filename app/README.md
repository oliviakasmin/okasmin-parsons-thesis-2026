frontend dataviz

technologies used

- React
- d3.js
- TypeScript

app structure

- Vite app root: `app/`
- HTML entrypoint: `app/index.html`
- React source: `app/src/`

build output

- Vite project root is `app/`
- generated deployable files are written to repo root `dist/`
